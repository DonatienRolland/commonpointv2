<div class="modal" id="notificationModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">

    <div class="modal-content col-lg-4 col-lg-offset-4" style="padding: 10px">
      <a class="close cross abs-right-top" data-dismiss="modal" id="close" aria-label="Close">+</a>
      <h4>Mes notifications</h4>
      <%= f.simple_fields_for :notification do |e| %>
        <div class="form-group">
          <%= e.label :invitation_prive, label:'Invitation Privé', class:"form-label", required: false %>
          <%= e.input :invitation_prive, label:false, class:"form-control", tabindex:"1" %>
        </div>
        <div class="form-group">
          <%= e.label :invitation_publique, label:'Invitation Publique', class:"form-label", required: false %>
          <%= e.input :invitation_publique, label:false, class:"form-control", tabindex:"1" %>
        </div>
         <div class="form-group">
          <%= e.label :message, label:'Message', class:"form-label", required: false %>
          <%= e.input :message, label:false, class:"form-control", tabindex:"1" %>
        </div>
        <div class="text-center">
          <%= f.button :submit, "Modifier", class:"btn btn-start-order" %>
        </div>
      <% end %>
    </div>
  </div>
